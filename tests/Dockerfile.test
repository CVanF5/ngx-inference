FROM alpine:3.18

# Install required tools
RUN apk add --no-cache \
    bash \
    curl \
    jq \
    docker-cli \
    docker-compose

WORKDIR /app

# Copy test scripts
COPY *.sh /app/
COPY configs/ /app/configs/

# Make scripts executable
RUN chmod +x /app/*.sh

CMD ["./test_docker_toggle.sh"]